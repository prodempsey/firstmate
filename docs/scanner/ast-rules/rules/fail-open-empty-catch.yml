id: fail-open-empty-catch
language: JavaScript
severity: error
message: Empty catch silently converts failure into success; handle or rethrow it.
rule:
  any:
    - pattern: try { $$$BODY } catch ($ERROR) {}
    - pattern: try { $$$BODY } catch {}
